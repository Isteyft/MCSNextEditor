# 全局 JSON 导入统一管理计划

1. `已执行` 建立统一导入核心类型与解析入口（`json-import` feature），先接入真实调用点。
2. `已执行` 抽离文件读取 + JSON 解析 + 默认回退为通用函数，替换 `useModuleLoaders` / `useProjectLifecycle` 重复逻辑。
3. `已执行` 抽离模块导入适配层（talent/affix/buff/item/skill/staticskill）并统一返回导入报告。
4. `已执行` 增加全局导入管理 hook（批量执行、错误汇总、状态管理）。
5. `已执行` 增加设置项：可配置导入根路径/工具库路径并持久化。
6. `已执行` 增加设置界面入口与“全局导入”界面，支持路径选择和导入执行。
7. `已执行` 统一状态提示文案和错误结构，替换设置/全局导入链路散落 `setStatus` 文案。
8. `已执行` 回归验证与清理：`typecheck/build` + 清理部分冗余解析代码。
